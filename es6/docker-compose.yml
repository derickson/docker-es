version: '2'
services:
  es_standalone:
    # build: elasticsearch_custom
    container_name: elasticsearch1
    image: docker.elastic.co/elasticsearch/elasticsearch-platinum:6.0.0-rc1
    environment:
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - xpack.security.enabled=true
      - "node.master=true"
      - "node.data=true"
      - xpack.ssl.key=/usr/share/elasticsearch/config/x-pack/certificates/elasticsearch1/elasticsearch1.key
      - xpack.ssl.certificate=/usr/share/elasticsearch/config/x-pack/certificates/elasticsearch1/elasticsearch1.crt 
      - xpack.ssl.certificate_authorities=/usr/share/elasticsearch/config/x-pack/certificates/ca/ca.crt
      - xpack.security.transport.ssl.enabled=true
      - xpack.security.http.ssl.enabled=false
    networks:
      app-tls:
      internal-tls:
        ipv4_address: 172.18.10.2
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    mem_limit: 1g
    volumes:
      - ./data/es_s1:/usr/share/elasticsearch/data
      - ./certificates:/usr/share/elasticsearch/config/x-pack/certificates
    ports:
      - 9200:9200

  kibana1:
    image: docker.elastic.co/kibana/kibana:6.0.0-rc1
    volumes:
      - ./certificates:/usr/share/kibana/config/certificates
    ports:
      - 5601:5601
    networks:
      - app-tls
    links:
      - "es_standalone:elasticsearch1"
    environment:
      - SERVER_PORT=5601
      - SERVER_HOST="0"
      - SERVER_NAME="dockermachine"
      - ELASTICSEARCH_URL=http://elasticsearch1:9200
      - elasticsearch.username=kibana
      - "elasticsearch.password=${KIBANA_PWD}"
      # - elasticsearch.ssl.certificateAuthorities=/usr/share/kibana/config/certificates/ca/ca.pem
      - xpack.security.enabled=true
      - xpack.monitoring.enabled=false
      - xpack.security.encryptionKey=something_at_least_32_characters_yo
      

networks:
  app-tls:
    driver: bridge
  internal-tls:
    driver: bridge
    ipam:
      driver: default
      config:
      - subnet: 172.18.10.0/24
        gateway: 172.18.10.1


